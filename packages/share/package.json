{
    "name": "playwright-interceptor-share",
    "version": "1.0.0",
    "private": true,
    "description": "Shared Playwright tests and helpers, imported by the version-specific Playwright runner packages.",
    "scripts": {
        "lint": "eslint .",
        "tsc": "tsc --noEmit"
    }
}
