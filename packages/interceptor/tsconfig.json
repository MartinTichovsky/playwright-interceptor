{
    "compilerOptions": {
        "types": ["node"],
        "lib": ["ES2021", "DOM", "DOM.Iterable"]
    },
    "exclude": ["node_modules", "**/*.d.ts"],
    "extends": "../../tsconfig.json",
    "include": ["**/*.ts"]
}
